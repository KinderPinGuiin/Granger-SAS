{% block form_row %}
    {{ form_widget(form) }}
    {{ form_label(form) }}
    {% if errors|length > 0 %}
        {% for error in errors %}
            <span class="error">{{ error.message }}</span>
        {% endfor %}
    {% endif %}
{% endblock %}